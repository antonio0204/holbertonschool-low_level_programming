#include <stdio.h>
#include <time.h>
/**
  * main - the function main be  generates a random number  a validate
  *
  * Return: Always 0 (Sucess)
 */

int main(void)
{
	char az = 'a';

	for ( ; az <= 'z'; az++)
	putchar (az);
	putchar ('\n');
	return (0);
}
