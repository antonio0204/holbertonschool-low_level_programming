#include "holberton.h"
/**
* mul - detects if it's the case is Upper.
* @a : takes the first number
* @b : takes the second number
* Return: the multiplication between of first and second
*
*/

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
